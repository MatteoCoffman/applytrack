{
  "name": "applytrack",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "sam:build": "cd backend && sam build",
    "sam:deploy": "cd backend && sam deploy"
  }
}
